<?xml version="1.0" encoding="utf-8"?>
<actions>
    <!-- Google Assistant App Actions for proper voice command recognition -->
    
    <!-- Handle "Hey Google, talk to AutoChat" -->
    <action intentName="actions.intent.START_EXERCISE">
        <fulfillment urlTemplate="autochat://conversation{?query}">
            <parameter-mapping
                intentParameter="exercise.name"
                urlParameter="query" />
        </fulfillment>
    </action>
    
    <!-- Handle "Hey Google, ask AutoChat about the weather" -->
    <action intentName="actions.intent.GET_WEATHER">
        <fulfillment urlTemplate="autochat://weather{?location}">
            <parameter-mapping
                intentParameter="location"
                urlParameter="location" />
        </fulfillment>
    </action>
    
    <!-- Handle "Hey Google, ask AutoChat [question]" -->
    <action intentName="actions.intent.GET_THING">
        <fulfillment urlTemplate="autochat://ask{?query}">
            <parameter-mapping
                intentParameter="thing.name"
                urlParameter="query" />
        </fulfillment>
    </action>
    
    <!-- Handle general conversation starters -->
    <action intentName="actions.intent.OPEN_APP_FEATURE">
        <fulfillment urlTemplate="autochat://conversation{?feature}">
            <parameter-mapping
                intentParameter="feature"
                urlParameter="feature" />
        </fulfillment>
    </action>
</actions>
