<?xml version="1.0" encoding="utf-8"?>
<actions>
    <!-- Main conversation action for "talk to AutoChat" -->
    <action intentName="actions.intent.OPEN_APP_FEATURE">
        <fulfillment urlTemplate="autochat://conversation{?query}">
            <parameter-mapping
                intentParameter="feature"
                urlParameter="query" />
        </fulfillment>
    </action>
    
    <!-- Direct query action for "ask AutoChat about..." -->
    <action intentName="actions.intent.GET_THING">
        <fulfillment urlTemplate="autochat://ask{?query}">
            <parameter-mapping
                intentParameter="thing.name"
                urlParameter="query" />
        </fulfillment>
    </action>
    
    <!-- Weather-specific action -->
    <action intentName="actions.intent.GET_WEATHER">
        <fulfillment urlTemplate="autochat://weather{?location}">
            <parameter-mapping
                intentParameter="location"
                urlParameter="location" />
        </fulfillment>
    </action>
    
    <!-- General question action -->
    <action intentName="actions.intent.ASK_QUESTION">
        <fulfillment urlTemplate="autochat://question{?query}">
            <parameter-mapping
                intentParameter="question"
                urlParameter="query" />
        </fulfillment>
    </action>
    
    <!-- Text query action for general queries -->
    <action intentName="actions.intent.TEXT">
        <fulfillment urlTemplate="autochat://text{?query}">
            <parameter-mapping
                intentParameter="text"
                urlParameter="query" />
        </fulfillment>
    </action>
</actions>
